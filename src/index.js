const mdLinks = (path, option) => {

// validación de path
const validatePath = new Promise ((resolve, reject) => {
    if ('ruta valida') {
        resolve(true)
    } else {
        reject(false)
    }
})

// validación de option
// validación de path absoluta o relativa
// validación de directorio
// validación de archivo md

}

module.exports = { mdLinks }
